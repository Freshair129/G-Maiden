# G-Maiden Tauri v2 + React Monorepo — Scaffold Setup (G0.1)

> ⚠️ **เอกสารเชิงประวัติ (scaffold phase G0.1).** บันทึกตอนตั้งโปรเจกต์ครั้งแรก — บาง choice
> เปลี่ยนไปแล้ว (เช่น G-Log = JSONL ไม่ใช่ `rusqlite`/SQLite). สถานะจริงล่าสุดดูที่ `AGENTS.md`.

## What was scaffolded

This document verifies the completion of task **G0.1: Scaffold Tauri v2 + React/Vite/Tailwind monorepo**.

### Root Structure
- `package.json` — pnpm workspace root with monorepo config
- `src-tauri/` — Rust core (Tauri v2 + tokio + axum ready)
- `src/` — React + Vite + TypeScript + TailwindCSS frontend
- `assets/voice-cache/` — Audio cache directory (for critical alerts)
- `models/` — SLM + Piper voice models (lazy-load, not committed)
- `tests/perf/` — Performance test harness placeholder

### Tauri v2 Setup (src-tauri/)
- ✅ `Cargo.toml` — Updated with tauri v2, tokio, serde, rusqlite, crossbeam
- ✅ `tauri.conf.json` — Dev/build paths, window config, CSP, bundle targets (MSI/NSIS)
- ✅ `build.rs` — Tauri build script
- ✅ `src/main.rs` — Tauri app builder scaffolded (ready to receive modules)
- ✅ `src/audio/` — Placeholder for G4.6 narration queue
- ✅ `src/glog/` — Placeholder for G-Log SQLite layer

### React Frontend (src/)
- ✅ `package.json` — React 18, Vite, TypeScript, TailwindCSS, React Query, Zustand
- ✅ `vite.config.ts` — Configured to build into `src-tauri/gen/web`
- ✅ `tsconfig.json` + `tsconfig.node.json` — TypeScript strict mode
- ✅ `tailwind.config.js` — Ice-glass theme colors (`ice-dark`, `ice-panel`)
- ✅ `postcss.config.js` — Autoprefixer + TailwindCSS
- ✅ `index.html` — Vite entry (loads React at #root)
- ✅ `src/main.tsx` — React DOM mount point
- ✅ `src/App.tsx` — Glassmorphism welcome component
- ✅ `src/index.css` — Tailwind directives + dark theme base
- ✅ Directory stubs:
  - `src/overlay/` — Overlay components (G-Sensory HUD layer)
  - `src/dashboard/` — Dashboard components (control/stats)
  - `src/components/` — Shared UI components
  - `src/store/` — Zustand + React Query state (g-signal alerts, resource stats)
- ✅ `.eslintrc.cjs` — ESLint config (React Hooks, TypeScript)

### Monorepo Config
- ✅ Root `package.json` scripts:
  - `pnpm dev` → `pnpm tauri dev` (launches dev server + Tauri window)
  - `pnpm build` → Frontend build + Tauri binary build
  - `pnpm build:fe` / `pnpm build:rust` — Separate builds
- ✅ pnpm workspaces linking `src` and `src-tauri`
- ✅ `.gitignore` — Node modules, dist, Rust target/, build artifacts

## Verification: How to Test

Run these commands from `G:\G-Maiden`:

```bash
# 1. Install monorepo dependencies
pnpm install

# 2. Start dev server (scaffolding acceptance test)
pnpm dev
# or
pnpm -C src-tauri tauri dev

# Expected: Tauri window opens showing the welcome screen
```

**Acceptance Criteria:** ✅ `pnpm tauri dev` opens a window displaying the G-Maiden welcome UI.

If you see a glassmorphic panel with "G-Maiden" title and checkmarks, the scaffold is complete.

## Next Steps (Scoped Outside G0.1)

- **G1.x**: GSI server (port 3000), game state parsing
- **G2.x**: Minimap CV (DXGI capture + icon detection)
- **G3.x**: G-Sentry/G-Motion/G-Signal logic (latency-critical)
- **G4.x**: Audio + TTS integration (Piper, pre-rendered cache)
- **G5.x**: Overlay rendering (transparent, click-through, no minimap overlap)
- **G6.x**: Cloud integration (Gemini persona + brain router)
- **Tests**: Latency harness, CPU/RAM/FPS governor tests

## Tech Stack Summary

| Layer | Tech | Notes |
| --- | --- | --- |
| Desktop | Tauri v2 | WebView2, transparent overlay, low footprint |
| Rust Core | tokio + axum + crossbeam | Critical path (≤300ms gank warnings) |
| Frontend | React 18 + Vite + TS | Shared overlay + dashboard UI |
| Styling | TailwindCSS | Ice-glass theme (glassmorphism) |
| State | Zustand + React Query | Overlay alerts + dashboard data sync |
| Database | SQLite (rusqlite) | G-Log (local, privacy-first) |
| Build | pnpm + cargo + Tauri bundler | Monorepo scripts; MSI/NSIS on Windows |

---

Generated by G0.1 scaffold task.
