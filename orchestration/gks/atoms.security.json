{
  "block": "Genesis::GMaiden-SEC001-AuthHardening",
  "manifest": {
    "manifest_version": "0.1.0",
    "source": "docs/audits/SEC-001-auth-identity-hardening.md (live gstore audit 2026-07-04)",
    "daci": {
      "driver": "Persona::ARCHON",
      "approver": ["Persona::Boss", "Persona::ATHER"],
      "contributor": ["Persona::KIN", "Persona::VIBE"],
      "informed": ["Persona::RKOI", "Persona::GHOST"]
    },
    "_note": "Auth/identity hardening — HARD predecessor of CR-003 economy. Compile: `node gks/compile.mjs gks/atoms.security.json`. Findings F1-F8 confirmed on live gstore. Part B migration is BREAKING and gated behind requiresConfirm — it needs the mint-gid Edge Fn + client changes in the same deploy. Live DB writes require Boss approval (shared ecosystem DB)."
  },
  "atoms": [

    {
      "id": "eval--pgtap-identity-lock",
      "type": "eval",
      "displayName": "pgTAP: identity columns unforgeable (F1) — RED first",
      "layer": "L3-Storage",
      "phase": "P0",
      "tier": "H1",
      "role": "coder",
      "deps": [],
      "est": 1,
      "moscow": "must",
      "body": "Write supabase/tests/sec001_identity_lock.sql BEFORE the Part B migration (must be RED on today's schema): as an authenticated user, UPDATE own profiles SET generation='B' -> must be denied; SET gid_code='G-FFAKE' -> denied; SET role='admin' (after role exists) -> denied; SET display_name='ok' -> allowed. Proves F1 is closed. Doc: SEC-001 §3. Code: supabase/tests/sec001_identity_lock.sql.",
      "accept": "File runs; the deny-assertions FAIL on the current schema (proving the hole is real) and PASS after config--migration-identity-hardening Part B.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md"],
      "excludes": ["tests only — no schema change"],
      "state": "new"
    },
    {
      "id": "config--migration-identity-hardening-part-a",
      "type": "config",
      "engineType": "code",
      "displayName": "Migration Part A — revokes + search_path (safe)",
      "layer": "L3-Storage",
      "phase": "P0",
      "tier": "H1",
      "role": "coder",
      "deps": [],
      "est": 1,
      "moscow": "must",
      "requiresConfirm": true,
      "body": "Apply Part A of supabase/migrations/20260704120000_sec001_identity_hardening.sql: revoke EXECUTE on alloc_cohort_seq + handle_new_user from anon/authenticated (F2/F3), revoke all on gid_counters + profiles from anon (F4), pin touch_updated_at search_path (F7). Non-breaking. Doc: SEC-001 §2 Phase A. Code: supabase/migrations/20260704120000_sec001_identity_hardening.sql.",
      "accept": "After apply: get_advisors(security) no longer flags 0011/0028/0029 for these functions; signup still mints a cohort seq (trigger path unaffected); existing signed-in profile reads still work.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md"],
      "excludes": ["Part A only — do NOT run Part B", "live apply needs Boss approval"],
      "state": "new"
    },
    {
      "id": "config--auth-leaked-password",
      "type": "config",
      "displayName": "Enable leaked-password protection (F8)",
      "layer": "L1-Policy",
      "phase": "P0",
      "tier": "H0",
      "role": "worker",
      "deps": [],
      "est": 1,
      "moscow": "should",
      "requiresConfirm": true,
      "body": "Auth config toggle (not SQL): Dashboard/API -> Auth -> Password -> enable HaveIBeenPwned check. Near-N/A today (Google-OAuth only) but free and future-proofs any email path. Doc: SEC-001 F8. Code: none (config).",
      "accept": "get_advisors(security) no longer returns auth_leaked_password_protection.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md"],
      "state": "new"
    },

    {
      "id": "feature--ef-mint-gid",
      "type": "feature",
      "displayName": "Edge Fn mint-gid — server-authoritative GID (reuses gid.ts)",
      "layer": "L2-Feature",
      "phase": "P1",
      "tier": "H2",
      "role": "coder",
      "deps": ["config--migration-identity-hardening-part-a"],
      "est": 2,
      "moscow": "must",
      "body": "supabase/functions/mint-gid/index.ts: JWT auth, load the caller's profile (generation, cohort_seq, created_at), import the SAME src/src/gid.ts codec (generateGid) so the algorithm stays single-sourced per ADR-14, and UPDATE profiles SET gid_code=... WHERE id=auth.uid() AND gid_code IS NULL via service_role. Idempotent (null-guard). TDD self-contained: mint-gid.test.ts FIRST — mints once, second call is a no-op, missing seq -> error. Doc: SEC-001 §2 Phase B step 1. Code: supabase/functions/mint-gid/index.ts.",
      "accept": "deno test green (tests first); minting works with the profiles UPDATE(gid_code) grant revoked from the client; output GID byte-identical to gid.ts for the same source fields.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md", "src/src/gid.ts", "docs/architecture/adr/ADR-14-gid-account-identity.md"],
      "state": "new"
    },
    {
      "id": "feature--client-gid-mint-move",
      "type": "feature",
      "displayName": "Client: stop writing gid_code / email; call mint-gid",
      "layer": "L2-Feature",
      "phase": "P1",
      "tier": "H1",
      "role": "coder",
      "deps": ["feature--ef-mint-gid"],
      "est": 1,
      "moscow": "must",
      "body": "profile.ts: delete the client-side `update profiles set gid_code` (the .is('gid_code',null) block); instead invoke the mint-gid function when gid_code is empty, then read it back. auth.ts linkProfile: switch `.upsert({id,email,steamid64,account_id})` to `.update({steamid64,account_id}).eq('id',userId)` (row pre-exists via trigger; email is set server-side). Doc: SEC-001 §2 Phase B step 3. Code: src/src/profile.ts + src/src/auth.ts.",
      "accept": "npx tsc --noEmit + vitest pass; a fresh Google sign-in still yields a persisted G-F… GID; no client code writes generation/gid_code/cohort_seq/email anymore (grep proves it).",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md", "src/src/profile.ts", "src/src/auth.ts"],
      "state": "extend"
    },
    {
      "id": "config--migration-identity-hardening-part-b",
      "type": "config",
      "engineType": "code",
      "displayName": "Migration Part B — column-lock + role column (BREAKING)",
      "layer": "L3-Storage",
      "phase": "P1",
      "tier": "H2",
      "role": "coder",
      "deps": ["eval--pgtap-identity-lock", "feature--ef-mint-gid", "feature--client-gid-mint-move"],
      "est": 1,
      "moscow": "must",
      "requiresConfirm": true,
      "body": "Uncomment + apply Part B: revoke INSERT/UPDATE on profiles from authenticated, drop own_profile_insert, grant UPDATE(display_name,steamid64,account_id) only, add locked `role` column (default 'user', check user/creator/admin, no client grant). Closes F1 + makes CR-003's role safe from birth. Deploy in lockstep with the two feature atoms above. Doc: SEC-001 §2 Phase B step 2. Code: supabase/migrations/20260704120000_sec001_identity_hardening.sql.",
      "accept": "eval--pgtap-identity-lock GREEN (forgery denied, display_name allowed); mint-gid + sign-in still work end-to-end; get_advisors(security) clean for the account schema.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md"],
      "excludes": ["apply only after the mint-gid EF + client changes are live", "live apply needs Boss approval"],
      "state": "new"
    },

    {
      "id": "eval--vitest-secure-session-store",
      "type": "eval",
      "displayName": "Vitest: encrypted session storage adapter (F5) — RED first",
      "layer": "L2-Feature",
      "phase": "P2",
      "tier": "H1",
      "role": "coder",
      "deps": [],
      "est": 1,
      "moscow": "should",
      "body": "Write src/src/__tests__/secure-store.test.ts BEFORE the adapter: getItem/setItem/removeItem round-trip through a mocked Tauri encrypted store; asserts nothing is written to window.localStorage. Doc: SEC-001 §2 Phase C (F5). Code: src/src/__tests__/secure-store.test.ts.",
      "accept": "vitest red (adapter missing) with the no-localStorage assertion encoded.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md"],
      "excludes": ["tests only"],
      "state": "new"
    },
    {
      "id": "feature--secure-session-store",
      "type": "feature",
      "displayName": "Encrypted Supabase session storage adapter (F5)",
      "layer": "L2-Feature",
      "phase": "P2",
      "tier": "H2",
      "role": "coder",
      "deps": ["eval--vitest-secure-session-store"],
      "est": 2,
      "moscow": "should",
      "body": "src/src/secureStore.ts: a Supabase auth `storage` adapter backed by an encrypted Tauri store (tauri-plugin-store + OS DPAPI, or Stronghold), replacing plaintext WebView2 localStorage for the session. Wire into supabase.ts `auth.storage`. Graceful fallback to memory when not under Tauri (browser dev). Doc: SEC-001 §2 Phase C. Code: src/src/secureStore.ts + src/src/supabase.ts.",
      "accept": "eval--vitest-secure-session-store GREEN; after sign-in, the WebView2 localStorage holds no JWT/refresh token; session survives app restart.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md", "src/src/supabase.ts"],
      "state": "extend"
    },
    {
      "id": "guard--oauth-state-nonce",
      "type": "guard",
      "engineType": "micro",
      "displayName": "Pure fn: OAuth state nonce verify (F6)",
      "layer": "L3-Logic",
      "phase": "P2",
      "tier": "H1",
      "role": "coder",
      "deps": [],
      "est": 1,
      "moscow": "should",
      "body": "Pure core in src/src/oauthState.ts: `makeState(): string` (caller stores it) and `verifyState(expected: string|null, got: string|null): boolean` — true only when both present and equal; false on null/mismatch/reuse (caller clears expected after true). TDD: oauthState.test.ts FIRST, >=5 cases (match, mismatch, null expected, null got, empty). Wiring into auth.ts (attach state to signInWithOAuth, check on oauth-callback) is done in feature--secure-session-store's PR or a follow-up. Doc: SEC-001 F6. Code: src/src/oauthState.ts.",
      "accept": "vitest green (tests first); function is pure (no crypto side effects beyond a passed RNG or a fixed test seed), no DOM/Tauri imports.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md"],
      "excludes": ["one file + its test only", "no react/tauri imports — pure logic"],
      "state": "new"
    },
    {
      "id": "guard--open-url-allowlist",
      "type": "guard",
      "displayName": "open_url scheme/host allowlist (Rust)",
      "layer": "L3-Logic",
      "phase": "P2",
      "tier": "H1",
      "role": "coder",
      "deps": [],
      "est": 1,
      "moscow": "should",
      "body": "Harden src-tauri/src/main.rs open_url: only launch https:// URLs (reject file:/data:/custom schemes) so a crafted OAuth url can't shell out. Add a #[cfg(test)] unit test for the validator (accept https google/supabase host, reject file:// and non-url). Doc: SEC-001 §2 Phase C. Code: src-tauri/src/main.rs.",
      "accept": "cargo test green incl. the new validator test; a non-https url is refused and logged; the real Google/Supabase auth url still opens.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md", "src-tauri/src/main.rs"],
      "state": "extend"
    },

    {
      "id": "audit--sec001-signoff",
      "type": "audit",
      "displayName": "SEC-001 sign-off — advisors clean + gate into CR-003",
      "layer": "L1-Policy",
      "phase": "P3",
      "tier": "H1",
      "role": "worker",
      "deps": ["config--migration-identity-hardening-part-b", "config--migration-identity-hardening-part-a", "config--auth-leaked-password", "feature--secure-session-store", "guard--oauth-state-nonce", "guard--open-url-allowlist"],
      "est": 1,
      "moscow": "must",
      "body": "ATHER sign-off: re-run get_advisors(security) -> zero WARN/ERROR for the account schema; walk SEC-001 §3 verification table with recorded evidence; confirm F1 forgery is dead. This atom is the dependency edge SEC-001 -> CR-003 (economy builds only on the hardened base). Doc: SEC-001 §3/§4. Code: none (audit doc update).",
      "accept": "SEC-001 §3 table all green with evidence links; advisor run attached; CR-003 unblocked to start Phase 0.",
      "needs": ["docs/audits/SEC-001-auth-identity-hardening.md", "docs/change request/CR-003-account-phase1-wallet-billing.md"],
      "state": "new"
    }
  ]
}
