# Session — 2026-06-21 (turn 15) · autonomous wave runner

User ขอ "ระบบ autonimas ที่รันแต่ละ wave เอง พอจบ wave ก็เรียก agent คุมงานมา
review ถ้าผ่านก็รัน wave ต่อไป เรื่อย ๆ จนจบงาน". Engine มี Verify Gate ระดับ
task แล้ว + `runPool({mode:auto})` cascade แล้ว, แต่ขาด **wave-level barrier
+ supervisor**. turn นี้เติมส่วนนั้น.

## ⚠️ บทเรียนแรก: branch สลับระหว่าง session

`git log --all --oneline` ตอนเริ่ม turn เห็นแค่ main. แต่หลัง commit ครั้งแรก
ของ turn ระบบรายงาน branch ที่ลงเป็น `GVBR-1-anti-error-loop-l0l1` — user
checkout ไป feature branch ระหว่าง session. มี commits ของ user/agent อื่นที่
ผมยังไม่รู้:
- `bed6092` GoVibe/GenesisDB integration specs + ADRs (approved)
- `42ca3ec` anti-error loop L0+L1 — failure memory + prompt injection
- `b8694dd` anti-error loop L2 — grounded context via GRL retrieveContext

**กับดักใหม่**: เช็ค `git branch --show-current` ก่อน commit, ไม่ใช่แค่
`git log --all`. โชคดี commit ของผมเข้า branch ถูก context (orchestration
work อยู่บน feature branch อยู่แล้ว).

## สิ่งที่ทำ (commit `3c0bb86`)

### `orchestration/auto-wave.mjs` (ใหม่)

- `runAutonomous({maxWaves, supervisorModel, concurrency, onLog})`
- ลูป:
  1. `pickNextWave(state)` = DAG level แรกที่ยังมี task ไม่ done
  2. `runPool({mode:"wave"})` ปล่อย wave ทีละชุด (snapshot ครั้งเดียว)
  3. `waitForWave(ids)`: poll `poolStatus()` จนกว่า:
     - `running === 0 && !active` ติดต่อกัน `idleMs = 3000`
     - และทุก task ใน wave SETTLED (`done`/`failed`/`needs-rework`) หรือ
       เป็น `todo` + manual (skip)
  4. gather worker log + review log (tail 35/20) ของแต่ละ task
  5. spawn supervisor (`claude -p`) ผ่าน CONFIG.executor + auth env
  6. parse JSON บรรทัดเดียว (fail-safe: unparseable = hold, spawn error = hold)
  7. verdict `pass` → wave หน้า; `hold` → stop + write report
- prompt template สั่งซ้ำว่า **JSON only** + ห้าม pass ถ้ามี critical/major
- report markdown สรุปทุก wave + supervisor log แยกต่อ wave

### `orchestration/orchestrator.mjs` (modified)

- subcommand `auto-wave [--max-waves N] [--supervisor MODEL] [--max N]
  [--dry-run]`
- subcommand `stop` = `stopPool()`
- dry-run print wave plan เหมือน `run` dry-run (smoke verify DAG + module load)

## Verify

| Layer | ผ่าน |
|-------|------|
| Module load (import engine.mjs) | clean |
| Dry-run: `node orchestrator.mjs auto-wave --dry-run` | **11 waves planned** ถูกต้องตาม backlog ปัจจุบัน |

ไม่รัน live: กิน Plan quota จริง + แก้ repo จำนวนมาก → user trigger เอง
เมื่อพร้อม.

## ความต่าง 3 ระดับของระบบ review

| ระดับ | กลไก | ที่ไหน |
|------|------|--------|
| **task** | Verify Gate (ADR-O-001) — reviewer อิสระตรวจ output เทียบ acceptance | `engine.mjs::runReview` per `executeWithReview` |
| **wave** (ใหม่) | supervisor review หลัง wave จบ — cross-task consistency, next-wave readiness | `auto-wave.mjs::runAutonomous` |
| **global** | (ยังไม่มี) — รีวิวภาพรวมทั้ง backlog | (future) |

supervisor มอง pattern ที่ Gate รายตัวไม่เห็น: greeting/placeholder ที่ผ่าน
รายตัวแต่รวมกันเป็น false-done, dependency ที่เป็น skeleton, risk pattern.

## หลักการที่ใช้ใน turn นี้

1. **ไม่แตะไฟล์ที่ user กำลังแก้** — engine.mjs, master.rs, gsi.rs, log.rs,
   main.rs ทั้งหมด user modify อยู่. ผมเขียน **module ใหม่** วางบน engine
   ผ่าน public exports เท่านั้น.
2. **Dry-run ก่อน live** สำหรับ feature ที่ side-effect ใหญ่ (orchestrator
   trigger ทั้ง backlog = แก้ไฟล์ทั่ว repo) — verify wiring ไม่ใช่
   verify behavior.
3. **Fail-safe defaults**: unparseable verdict = hold, spawn error = hold,
   timeout = hold. supervisor "ลังเล" = หยุด ดีกว่า "ลังเล" = ผ่าน.

## State ปลาย turn

- Branch `GVBR-1-anti-error-loop-l0l1` ahead of origin (orchestration work).
- main มี commits ของ turn 1-14 (ผม) + บางส่วนของ user.
- Working tree: user มี mod ใน Rust/log + new untracked orchestration files
  + telemetry.rs.
- งานต่อ: user ตัดสินใจ trigger `auto-wave` live หรือไม่ + WAV asset +
  G-Sentry ONNX (ตาม spike S-1 turn 14).
